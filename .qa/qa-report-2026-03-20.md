# QA Report: Auto-select fixed overhangs + sub-block collision check

| Field | Value |
|-------|-------|
| **Date** | 2026-03-20 |
| **Tier** | Standard |
| **Scope** | PR #47 — branch `worktree-260319-automatic-oh-junctions` (11 files, +764/-119 lines) |
| **Mode** | Fix |

## Health Score: 92/100 (A)

Previous baseline: 91 (A) on 2026-03-18  [+1 point]

| Category | Score | Prev | Δ | Notes |
|----------|-------|------|---|-------|
| Test Coverage | 9/10 | 9 | = | 11 new tests for auto-select functions |
| Test Pass Rate | 10/10 | 10 | = | 461+ tests, 0 failures across 6 test files |
| Data Integrity | 10/10 | 9 | +1 | GRIN2A fix verified: 0.9406 → 1.0000 BsaI set fidelity |
| Edge Case Handling | 9/10 | 9 | = | Large blacklists, palindromes, NULL inputs, NNNN rejection all handled |
| Reproducibility | 10/10 | 9 | +1 | 5 runs produce identical results (both BsaI and PaqCI) |
| Documentation | 8/10 | 8 | = | Docstrings updated, config template documented |

## Bugs Found and Fixed

**None.** Clean implementation — no bugs discovered during QA.

## Key Verification

### GRIN2A BsaI Set Fidelity Fix (the primary motivation)

| | oh_L | oh4 | BsaI Set Fidelity | Per-OH Fidelity |
|---|------|-----|-------------------|-----------------|
| **Before** (old individual-score selection) | TGAA | AGAA | **0.9406** | TGAA: 0.9507, AGAA: 0.9894 |
| **After** (new pair set-fidelity selection) | TGAA | AAGA | **1.0000** | TGAA: 1.0000, AAGA: 1.0000 |

The 4.9% cross-ligation between TGAA and AGAA is eliminated by selecting AAGA instead.

### OOGGA Sub-block Compatibility

Verified that `optimize_split_points()` and `find_cassette_split_points()` now reject near-matches (positional identity >2/4) using the OOGGA compatibility matrix. Standalone test confirmed all junction OHs pass OOGGA check against existing reaction overhangs.

## Tests Generated

11 new tests added:

1. `auto_select_bsai_pair: both auto returns set_fidelity == 1.0`
2. `auto_select_bsai_pair: one specified, one auto preserves specified`
3. `auto_select_bsai_pair: both specified validates and warns`
4. `auto_select_bsai_pair: TGAA+AGAA cross-ligation detected`
5. `auto_select_paqci_pair: both auto returns valid non-colliding pair`
6. `auto_select_paqci_pair: both specified validates`
7. `auto_select_paqci_pair: identical pair rejected`
8. `plan_assembly with oh_L=auto and oh4=auto returns valid plan`
9. `plan_assembly with specified oh_L auto-selects oh4 with set fidelity`
10. `optimize_split_points uses OOGGA compat check, not just exact match`
11. `find_cassette_split_points uses OOGGA compat check`

## Remaining Issues

- `test-geneblock-design.R` times out in CI (>3 min) due to long-gene tests — pre-existing, not a regression
- Full `test_dir()` exceeds 10 min timeout — pre-existing

## Recommendations

1. **Split long-gene tests** into a separate `test-geneblock-design-long.R` file so quick tests can run in CI
2. **Add AKAP11 integration test** to verify tile 9 BsmBI fidelity improvement (requires running the full pipeline)
3. Consider adding a **warning when user-specified oh_L is palindromic** in `auto_select_bsai_pair()` (currently only caught by `validate_config()`)
